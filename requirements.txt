# Python deps for the TIPS/breakeven total-return build.
# Create the env:   python -m venv .venv
# Activate (Win):   .venv\Scripts\activate
# Install:          pip install -r requirements.txt
#
# NOTE: blpapi is NOT on PyPI. Install it from Bloomberg's package repo:
#   pip install --index-url https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi
# (requires an active Bloomberg Terminal logged in on this machine; no extra account)
pandas>=2.2
pyarrow>=15
matplotlib>=3.8
requests>=2.31
openpyxl>=3.1   # Excel export (export.py: workbook write + amber auction highlighting)
statsmodels>=0.14   # hedge regression diagnostics: RLM (Huber), QuantReg, OLSInfluence (hedge_outliers.py)
scikit-learn>=1.4   # robust regressors: TheilSen, RANSAC, HuberRegressor (hedge_outliers.py)
